// TODO (UX/UI Team): Implement ChatBox component
// Props: store_id, user_id
// Gọi sendChatMessage() từ lib/api.ts
// Hiển thị ChatResponse.message + ChatResponse.cards (MenuCard component)
// Disclaimer: nếu response.disclaimer → hiển thị warning banner
export {};
