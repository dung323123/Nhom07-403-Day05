"""
Tool: allergen_check
Lọc menu theo allergen của user
Input:  allergen (str), items (List[MenuItem])
Output: safe_items (List[MenuItem]), unsure_items (List[MenuItem])
        unsure = items có allergens = null (chưa có data)
"""
def allergen_check(allergen: str, items: list) -> dict:
    # TODO (Tools Team): implement
    # QUAN TRỌNG: items có allergens=null → đưa vào unsure_items, KHÔNG vào safe_items
    raise NotImplementedError
