from fastapi import FastAPI

app = FastAPI(title="XanhSM Tools Service", version="0.1.0")

# TODO (Tools Team): expose HTTP endpoints nếu ai-agent gọi qua HTTP
# Hoặc dùng trực tiếp như Python package (import từ tools/)
